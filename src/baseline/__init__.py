"""Baseline experiment package.

Each baseline lives in its own subpackage and reuses MSL mainline components
without modifying them. Available baselines:

- randomSL: random-scheduling Split Learning (src/baseline/randomSL)
"""
